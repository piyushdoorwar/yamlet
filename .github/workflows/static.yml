# Deploys the marketing site (site/) to GitHub Pages.
name: Deploy site to Pages

on:
  # Deploy on pushes to the default branch (so site edits go live).
  push:
    branches: ["main"]
    paths:
      - "site/**"
      - ".github/scripts/generate-site-releases.js"
      - ".github/workflows/static.yml"
  # Re-deploy after a build/release run finishes, to refresh the releases manifest.
  workflow_run:
    workflows: ["Build artifacts"]
    types: [completed]
  # Allow manual runs from the Actions tab.
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

# Allow one concurrent deployment; don't cancel an in-progress production deploy.
concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  deploy:
    if: ${{ github.event_name != 'workflow_run' || github.event.workflow_run.conclusion == 'success' }}
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v5
        with:
          ref: ${{ github.event.repository.default_branch }}
      - name: Generate release manifest
        env:
          GITHUB_TOKEN: ${{ github.token }}
        run: node .github/scripts/generate-site-releases.js
      - name: Setup Pages
        uses: actions/configure-pages@v5
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: "site"
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5
